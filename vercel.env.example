# Vercel Dashboard → Settings → Environment Variables 에 아래 값을 등록하세요.
# 로컬 .env 파일은 사용하지 않습니다.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
TAVILY_API_KEY=

SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
